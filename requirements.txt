# Offline Knowledge Hub — Python dependencies
# Install with: pip install -r requirements.txt

requests>=2.31.0
flask>=3.1.0
werkzeug>=3.1.0
libzim>=3.13.0            # reads .zim files in-process — no kiwix-serve.exe needed
onnxruntime-genai>=0.15.2 # small offline LLM inference — no vendor binary needed
pystray>=0.19.5           # system tray icon (Open Portal / Open Admin / Quit)
Pillow>=12.0.0             # tray icon image loading
